# SEC-PD — gepinnte Abhängigkeiten (Home UND Bank identisch halten!)
# Pins getestet mit Python 3.10–3.12. Vor dem Wheel-Bundle die Python-Version
# des Bank-Servers prüfen (python3 -V) und PY_VERSION im Bundle-Skript setzen.

# Kern (offline zwingend nötig)
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.4.2
joblib==1.4.2
pydantic==2.7.4
requests==2.32.3

# NLP-Assets-Vorbereitung (optional genutzt; Daten via build_offline_bundle.sh)
nltk==3.9.1

# Nur Home-Setup relevant (EDGAR-Rohdownloads); Wheel wird trotzdem gebündelt
sec-edgar-downloader==5.0.2

# Entwicklung/Tests
pytest==8.2.2
