# SEC-PD — gepinnte Abhängigkeiten (Home UND Bank identisch halten!)
# Getestet für Python 3.12 (Home) und 3.14 (Bank). Die alten Pins
# numpy 1.26 / pandas 2.2 / sklearn 1.4 haben keine cp314-Wheels.
# Vor dem Wheel-Bundle: python3 -V auf der Bank, dann
#   PY_VERSION=3.14 PLATFORM=manylinux_2_28_x86_64 bash scripts/build_offline_bundle.sh

# Kern (offline zwingend nötig)
numpy==2.3.5
pandas==2.3.3
scikit-learn==1.7.2
joblib==1.5.3
pydantic==2.13.4
requests==2.32.3

# Entwicklung/Tests
pytest==8.4.2
